# Scanner training data only. Do not deploy this manifest.
apiVersion: v1
kind: Pod
metadata:
  name: insecure-fixture
spec:
  containers:
    - name: insecure
      image: alpine:3.18.0
      command: ["sh", "-c", "sleep 3600"]
      securityContext:
        privileged: true
        runAsUser: 0
